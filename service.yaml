apiVersion: v1
kind: Service
metadata:
  name: eks-app-service
  namespace: app
spec:
  selector:
    app: eks-app
  ports:
    - port: 80        # EXTERNAL LB PORT
      targetPort: 3000  # <— POD PORT
  type: LoadBalancer
